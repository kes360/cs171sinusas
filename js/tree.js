class TreeVis {
    constructor(parentElement, nodeData, linkData) {
        this.parentElement = parentElement;
        this.nodeData = nodeData;
        this.linkData = linkData;

        this.initVis();
    }

    initVis() {
        let vis = this;

        vis.margin = {top: 10, right: 50, bottom: 10, left: 50};
    }

    updateVis() {
        let vis = this;

    }
}